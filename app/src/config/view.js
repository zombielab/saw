"use strict";

import path from "path";

export default {
    paths : [
        path.resolve(__dirname, "../../", "resources", "views")
    ],
    compiled: path.resolve(__dirname, "../../../", "storage", "views")
};